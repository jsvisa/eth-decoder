# Backend API endpoint (hidden from frontend)
BACKEND_URL=http://x.y.z.z
# The API key used to authenticate writes to the backend is configured in the
# Settings page (Backend API Key), not in this file.
